Description:
    Generates a migration for te Comments table, and an Akismet config file.

Example:
    ./script/generate has_comments

    This will create:
        db/migrate/xxxx_create_comments.rb
        config/akismet.yml
